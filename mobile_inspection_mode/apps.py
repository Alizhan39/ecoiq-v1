from django.apps import AppConfig


class MobileInspectionModeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'mobile_inspection_mode'
    verbose_name = 'EcoIQ Mobile / iPad Inspection Mode'
