# Patent and Innovation Agent — Evaluation Criteria

Pass requires a conditional (never support) position whenever a candidate's only evidence is early-innovation layer.

Every test case in `test_cases.json` must produce the `expected_output`
fields shown; any mismatch is a failing eval, not a matter of degree.
