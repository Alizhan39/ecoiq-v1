# Community Impact Agent — Evaluation Criteria

Pass requires a `conditional` or `oppose` position (never `support`) whenever a vulnerability keyword is present.

Every test case in `test_cases.json` must produce the `expected_output`
fields shown; any mismatch is a failing eval, not a matter of degree.
