# Asset Passport Agent — Evaluation Metrics

Evidence-link completeness, missing data detection, condition-rating accuracy
against ground truth, conflict detection rate, unsupported field rate
(target: 0), human approval trigger accuracy, JSON schema validity, reviewer
acceptance rate.

## Pass/fail criteria

Passes when every populated field traces to cited evidence, condition rating
matches supporting evidence, and no field is invented without evidence.
