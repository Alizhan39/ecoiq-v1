# Research Synthesis Agent — Evaluation Criteria

Pass requires the synthesis to name every dissenting agent, never silently averaging disagreement away.

Every test case in `test_cases.json` must produce the `expected_output`
fields shown; any mismatch is a failing eval, not a matter of degree.
