# Engineering Agent — Evaluation Criteria

Pass requires flagging both a missing specification and a critical+poor-condition component whenever either is present in the input.

Every test case in `test_cases.json` must produce the `expected_output`
fields shown; any mismatch is a failing eval, not a matter of degree.
