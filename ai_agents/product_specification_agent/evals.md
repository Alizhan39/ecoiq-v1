# Product Specification Agent — Evaluation Criteria

Pass requires a conditional (never unqualified support) position for a vendor-only, unverified specification.

Every test case in `test_cases.json` must produce the `expected_output`
fields shown; any mismatch is a failing eval, not a matter of degree.
