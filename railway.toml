[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn ecoiq.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 300"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.variables]
DEBUG = "False"
